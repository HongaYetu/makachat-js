node_modules/
# dist/ dos pacotes é versionado (distribuição por git: `npm install` no servidor
# resolve os file:../../makachat-js/packages/* sem passo de build). Só os exemplos ficam ignorados.
apps/*/dist/
.env
*.log
.DS_Store

# intermediates do gradle do módulo nativo (compilados localmente; nunca versionar)
packages/*/android/build/
packages/*/android/.cxx/
